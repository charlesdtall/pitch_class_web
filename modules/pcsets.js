// This file includes the arrays of all the PC Sets used in this "web".
// I have orgnized these arrays by length and then set name, prioritizing commonly used sets, and starting on PC {0} (i.e. C).

// Church Modes
let c_maj, d_dor, e_phr, f_lyd, g_mix, a_min, b_loc;
c_maj = d_dor = e_phr = f_lyd = g_mix = a_min = b_loc = [0, 2, 4, 5, 7, 9, 11];

let db_maj, eb_dor, f_phr, g_lyd, ab_mix, bb_min, c_loc;
db_maj = eb_dor = f_phr = g_lyd = ab_mix = bb_min = c_loc = [0, 1, 3, 5, 6, 8, 10]

let g_maj, a_dor, b_phr, c_lyd, d_mix, e_min, fs_loc;
g_maj = a_dor = b_phr = c_lyd = d_mix = e_min = fs_loc = [0, 2, 4, 6, 7, 9, 11];



const c_M = [0, 4, 7];

const c_wt = [0, 2, 4, 6, 8, 10]


export { c_maj, d_dor, e_phr, f_lyd, g_mix, a_min, b_loc, db_maj, eb_dor, f_phr, g_lyd, ab_mix, bb_min, c_loc, g_maj, a_dor, b_phr, c_lyd, d_mix, e_min, fs_loc, c_M, c_wt}